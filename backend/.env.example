# Runtime: Nest picks DATABASE_URL from LOCAL or RENDER based on DATABASE_PROFILE.
DATABASE_PROFILE=local

DATABASE_URL_LOCAL=postgresql://postgres:password@localhost:5432/tnews?schema=public
DATABASE_URL_RENDER=postgresql://user:password@host.region.render.com/dbname?sslmode=require

